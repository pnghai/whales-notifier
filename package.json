{
  "name": "whale-notifiers",
  "version": "1.0.0",
  "description": "A Whales checker",
  "main": "build/app.js",
  "scripts": {
    "start:build": "tsc -w",
    "start:run": "nodemon build/app.js",
    "dev": "concurrently npm:start:*",
    "start": "tsc -w && node build/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^15.12.4",
    "@types/websocket": "^1.0.2",
    "dotenv": "^10.0.0",
    "node-telegram-bot-api": "^0.53.0",
    "websocket": "^1.0.34"
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "nodemon": "^2.0.7"
  }
}
